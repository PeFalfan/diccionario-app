import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'diccionario-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
